import fs from 'node:fs'; import path from 'node:path'; import crypto from 'node:crypto';
const root=process.cwd(), out=path.join(root,'docs','audits','DEPLOYMENT_ATTESTATION.json'), mp=path.join(root,'docs','audits','SIGNED_RELEASE_MANIFEST.json');
const m=fs.existsSync(mp)?JSON.parse(fs.readFileSync(mp,'utf8')):{}; const manifestSha=m.manifestSha256||null;
const a={generatedAt:new Date().toISOString(),status:'EXTERNAL_REQUIRED',releaseCommit:process.env.RELEASE_COMMIT||m.releaseCommit||null,deploymentId:process.env.DEPLOYMENT_ID||null,deploymentTarget:process.env.DEPLOYMENT_TARGET||null,manifestSha256:manifestSha,artifactSetSha256:manifestSha,migrationRunId:process.env.MIGRATION_RUN_ID||null,deployedAt:process.env.DEPLOYED_AT||null,deployedBy:process.env.DEPLOYED_BY||null,attestationId:process.env.DEPLOYMENT_ATTESTATION_ID||null,credentialsPersisted:false,rule:'Deployment attestation is valid only when an external deployment system records the exact release commit, manifest hash, deployment identity, and migration execution identity.'};
const required=['releaseCommit','deploymentId','deploymentTarget','manifestSha256','migrationRunId','deployedAt','deployedBy','attestationId']; if(required.every(k=>String(a[k]||'').trim()))a.status='PASS'; fs.writeFileSync(out,JSON.stringify(a,null,2)+'\n'); console.log(`deployment-attestation: ${a.status}`); if(a.status!=='PASS')process.exitCode=1;
